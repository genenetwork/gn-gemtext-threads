# Masquerade: Masquerading as resource-owner Drops Some Privileges

## Tags

* type: bug
* status: closed, completed, fixed
* priority: high
* assigned: fredm, zachs, acenteno
* keywords: gn2, genenetwork2, auth, gn-auth

## Description

* Go to https://genenetwork.org/
* Log in using one of the system-administrator users
* Go to the "View Dashboard" page
* Select "Masquerade As"
* Select a user who has a 'resource-owner' privilege but is not a system-administrator
* Click the "Masquerade" button to masquerade as that user
* Now go to the user's dashboard
* Go to "Data and Access Management" page
* Click on one of the resources that the user is 'resource-owner' on
* Scroll down to the "Resource Data" section
* Select the "Users" tab
* See errors like:
  ```
  Users:   UNAUTHORIZED: AuthorisationError ; You do not have the appropriate privileges to list users.
  ```

This bug is weird, in that it does not uniformly show up for all cases.

After troubleshooting some more, this was caused by a subtle issue:

A resource-owner who is also not a group-leader has the appropriate privileges to assign roles to users, but they do not have the appropriate privilege to list users. This was, originally, based on the idea that resources would be owned by user-groups rather than users -- this is not really true.

The fix is to assign the `system:user:list` privilege to the `resource-owner` role itself to avoid this issue in the future.

=> https://git.genenetwork.org/gn-auth/commit/?id=de5487f92ab60a10fa72458c1f4b421d4f0a2674 Here is the fix.

The change above has been deployed and this issue can now be marked as closed.
